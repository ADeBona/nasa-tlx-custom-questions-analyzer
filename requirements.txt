matplotlib==3.11.1
numpy==2.5.3
pandas==3.0.5

# Optional: enables paired t-test/Wilcoxon/95% CI sections in analyze_results.py.
# Without it, those sections are skipped with a warning instead of crashing.
# scipy
